#!/usr/bin/zsh
while (tsp  | grep -v State | grep -v finished)
do
  echo "==============================================================================================================="
  tsp -c
done
